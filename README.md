# CloudCompare-Class-Based-LAS-LAZ-Subsampler
This script is intended to be run inside CloudCompare's Python Runtime plugin. It takes the currently selected point cloud, reads its Classification scalar field, subsamples configured classes, optionally recolors configured classes, and creates one final merged cloud inside CloudCompare.
